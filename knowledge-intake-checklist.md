# 知识收录检查表

## 记忆状态
- 状态：active
- 替代文件：无
- 上次复核：2026-04-30
- 下次复核触发条件：知识升级路径、证据元数据或 memory 状态机制变化时复核

## 目的
- 用于判断一条新知识是否值得进入 `ai-memory`。
- 目标不是尽量多记，而是只沉淀真正稳定、可复用、可升级的知识。
- 这份检查表用于在新增记忆前，快速判断“该不该记、记到哪、现在记还是以后再记”。

## 8 个问题

### 1. 这条知识是事实还是结论
- 只是过程片段、聊天内容、临时想法，不进入全局 memory。
- 只有当它已经形成稳定结论，才继续往下判断。

### 2. 它只对当前一个 task 有用，还是跨 task 有用
- 只对当前 task 有用：留在 task、handoff 或项目上下文中。
- 跨 task 仍然有价值：继续判断。

### 3. 它只对当前项目成立，还是对同类项目也成立
- 只对当前项目成立：进入项目内 `memory/`
- 对同类项目成立：进入全局 `domains/` 候选范围

### 4. 它是个人判断、公司方法，还是领域打法
- 体现你本人稳定取舍：`personal/`
- 体现公司交付与协作方式：`company/`
- 体现某一类项目的专业经验：`domains/`

### 5. 它是规则知识，还是输出模板
- 稳定原则、方法、边界：进入 `personal/company/domains`
- 输出骨架、表达格式：进入 `templates/`
- 还没坐实但值得观察：进入 `retros/`

### 6. 它已经被验证过几次
- 0 到 1 次：不升级，最多进入 `retros/`
- 同一项目反复成立：可进入项目 `memory/`
- 跨项目稳定成立：可升级到全局 `ai-memory`

### 7. 它会不会污染默认上下文
- 太长、太具体、领域强绑定、客户专属、未验证稳定的内容，不应进入默认读取集。
- 即使进入 `ai-memory`，也应按需读取，而不是默认加载。

### 8. 写进去后，别人或未来的你能不能直接用
正式 memory 至少要能回答：
- 适用范围
- 稳定结论
- 什么时候用
- 什么时候不要用
- 来源证据
- 验证次数
- 跨项目次数
- 最后验证日期
- 待验证问题
- 下次复核时间或触发条件

如果做不到，说明这条知识还不够成熟。

## 证据元数据
进入 `retros/`、项目 `memory/` 或全局正式 memory 的知识，建议带上以下元数据：
- 状态：candidate / active / deprecated
- 验证次数：0、1、2+
- 跨项目次数：0、1、2+
- 来源项目或任务
- 最后验证日期
- 待验证问题
- 下次复核时间或触发条件
- 替代文件或替代规则（仅 deprecated 需要）

元数据用途：
- 判断是否能从 `retros/` 升级到正式 memory
- 判断是否应进入默认读取集
- 判断旧规则是否需要降级或淘汰

## 放置规则
- 当前 task 专用：留在 `tasks/` 或 handoff
- 当前项目专用：进入项目内 `memory/`
- 个人稳定判断：进入 `personal/`
- 公司交付方法：进入 `company/`
- 领域稳定打法：进入 `domains/`
- 输出骨架：进入 `templates/`
- 还没坐实：进入 `retros/stable-patterns.md`

## 升级路径
- 对话 / 临时分析
- task 或 handoff
- 项目内 `memory/`
- `retros/stable-patterns.md`
- `personal/company/domains`

## 稳定判断口诀
- 一次性的，不进 memory
- 项目专属的，不进全局
- 没验证够的，先进 `retros`
- 能复用、能解释、能限定边界的，才升级

## 什么时候用
- 想往 `ai-memory` 新增内容时
- 复盘后挑选哪些知识值得升级时
- 不确定一条经验该放哪时

## 不适用场景
- 当前只是简单执行任务，还没有形成知识结论
- 当前只是复制现有文件结构，不涉及新知识沉淀

## 最后复核
- 2026-04-30
